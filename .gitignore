# Binaries (anchored to repo root — `dingdong-cli` without the leading slash
# also matched the cmd/dingdong-cli/ source directory, which is the bug that
# kept the CLI from ever being pushed).
/bin/
/dingdong
/dingdong-cli

# Local secrets — never commit
deploy/k8s/secret.yaml
.env
.env.local

# Editor / OS noise
.DS_Store
.idea/
.vscode/

# Harness session state (not source)
.context/

# Go cruft
*.test
*.out
coverage.txt
