{
  "name": "dingdong-ui-tests",
  "private": true,
  "version": "0.0.0",
  "description": "JS test harness for internal/ui/static/app.js (vitest + happy-dom).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "happy-dom": "^15.11.0",
    "vitest": "^2.1.4"
  }
}
