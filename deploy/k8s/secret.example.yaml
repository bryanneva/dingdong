# Copy to deploy/k8s/secret.yaml and fill in. secret.yaml is gitignored.
# Generate a token: `openssl rand -hex 32`
apiVersion: v1
kind: Secret
metadata:
  name: dingdong-token
  namespace: dingdong
type: Opaque
stringData:
  token: "REPLACE_ME"
